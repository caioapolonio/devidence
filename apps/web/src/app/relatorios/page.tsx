import { PageHeader } from "@/components/PageHeader";

export default function ReportsPage() {
  return (
    <PageHeader
      title="Relatórios"
      subtitle="Geração assistida com evidências rastreáveis, revisão editorial e exportação do PDF 16:9."
      pending="Preenchida quando o gerador e o renderizador de PDF forem portados."
    />
  );
}
