import { PageHeader } from "@/components/PageHeader";

export default function ReportsPage() {
  return (
    <PageHeader
      title="Reports"
      subtitle="Assisted generation with traceable evidence, editorial review, and 16:9 PDF export."
      pending="Filled in once the generator and the PDF renderer are ported."
    />
  );
}
