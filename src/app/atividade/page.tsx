import { PageHeader } from "@/components/PageHeader";

export default function ActivityPage() {
  return (
    <PageHeader
      title="Atividade"
      subtitle="Commits, pull requests, reviews, issues e releases do período, separando minha contribuição da evolução do projeto inteiro."
      pending="Preenchida quando a atribuição e as três perspectivas forem portadas."
    />
  );
}
